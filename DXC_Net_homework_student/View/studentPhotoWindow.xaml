<Window x:Class="DXC_Net_homework_student.StudentPhotoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DXC_Net_homework_student"
        mc:Ignorable="d"
        Title="学生照片展示"
        Height="700"
        Width="1000"
        WindowStartupLocation="CenterOwner"
        WindowStyle="SingleBorderWindow"
        BorderThickness="3"
        BorderBrush="#FFA8DADC">
    <!-- 资源定义 -->
    <Window.Resources>
        <!-- 照片路径转换器 -->
        <local:PhotoPathConverter x:Key="PhotoPathConverter"/>
        <!-- 阴影效果 -->
        <DropShadowEffect x:Key="DropShadowEffect" BlurRadius="8" Opacity="0.4" Direction="270" Color="#FF8FB5A8"/>
        <DropShadowEffect x:Key="WindowShadowEffect" BlurRadius="15" Opacity="0.3" Direction="270" Color="#FF6B9080"/>
    </Window.Resources>

    <Grid Margin="10" Background="#FFE6F9E6" Effect="{StaticResource WindowShadowEffect}">
        <!-- 标题 -->
        <TextBlock Text="学生照片展示" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,10,0,20" Foreground="#FF4A7C59"/>

        <!-- 滚动查看器 -->
        <ScrollViewer Margin="0,50,0,0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <!-- 照片网格展示区域 -->
            <ItemsControl x:Name="studentsItemsControl" HorizontalAlignment="Stretch">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <!-- 使用WrapPanel进行自动换行布局，并绑定宽度到ScrollViewer -->
                        <WrapPanel Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=ScrollViewer}}" 
                                   HorizontalAlignment="Left" 
                                   VerticalAlignment="Top" 
                                   ItemWidth="180" 
                                   ItemHeight="220"
                                   Margin="0,0,0,0"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#FFA8DADC" BorderThickness="2" CornerRadius="8" Padding="8" Margin="10" Background="White" Effect="{StaticResource DropShadowEffect}">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                <!-- 照片容器 -->
                                <Border Width="150" Height="150" Background="#FFF7FFF7" CornerRadius="8" BorderBrush="#FFD1E8D1" BorderThickness="2">
                                    <Image Source="{Binding PhotoPath, Converter={StaticResource PhotoPathConverter}}" 
                                           Stretch="Uniform" 
                                           HorizontalAlignment="Center" 
                                           VerticalAlignment="Center"/>
                                </Border>
                                <!-- 学生姓名 -->
                                <TextBlock Text="{Binding Name}" 
                                           FontSize="16" 
                                           FontWeight="Bold" 
                                           HorizontalAlignment="Center" 
                                           Margin="0,10,0,0" 
                                           TextWrapping="Wrap" 
                                           TextAlignment="Center" 
                                           Foreground="#FF4A7C59"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Window>